"use client";
import {useState} from "react";
import {RadioGroup,RadioGroupItem} from "@/components/ui/radio-group";
import {problems} from "@/lib/content";
export function QuickDiagnosis(){const [value,setValue]=useState("");return <section id="diagnostico" className="section concrete diagnosis"><div><p className="eyebrow">02 / DIAGNÓSTICO RÁPIDO</p><h2>O que está<br/>acontecendo aí?</h2><p className="body-copy">Selecione o que mais se aproxima do problema. Vamos entender o cenário, um passo de cada vez.</p><div className={"diagnosis-line "+(value?"resolved":"")} aria-hidden="true"><span/></div></div><div><RadioGroup aria-label="O que está acontecendo?" value={value} onValueChange={setValue} className="diagnosis-options">{problems.map(([id,label])=><label key={id} className={"option "+(id===value?"selected":"")}><RadioGroupItem value={id}/><span>{label}</span><span aria-hidden="true">↗</span></label>)}</RadioGroup><div className="diagnosis-result" aria-live="polite">{value?<><p>Vamos organizar as informações para avaliação da equipe.</p><a className="button" href={"/check?problema="+value}>Continuar no Limpax Check ↗</a></>:<p className="muted">O Check prepara sua solicitação. A equipe confirma o atendimento.</p>}</div></div></section>}
