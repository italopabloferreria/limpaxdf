import assert from "node:assert/strict";const origin=process.argv[2]||"http://localhost:5173";const data={problem:"fossa",location:"Área externa fictícia",property:"residencia",urgency:"planejar",access:"Acesso de teste",region:"Região de teste",cep:"",name:"Teste automatizado",phone:"11999999999",email:"teste@example.com",notes:"Dados sintéticos",marketing:false,privacy:true,reviewAcknowledged:true,website:""};for(const path of ["/","/check","/privacidade","/cookies","/servicos/limpeza-de-fossas","/servicos/hidrojateamento","/servicos/desentupimento","/servicos/caixa-de-gordura","/servicos/controle-de-pragas","/sitemap.xml","/robots.txt","/media/hero.webp","/media/operation.webp","/api/health"]){const r=await fetch(origin+path);assert.equal(r.status,200,path);await r.arrayBuffer()}const key=crypto.randomUUID();const request=(body,source=origin)=>fetch(origin+"/api/check",{method:"POST",headers:{"Content-Type":"application/json","Origin":source,"Idempotency-Key":key},body:JSON.stringify(body)});assert.equal((await request(data,"https://invalid.example")).status,403);assert.equal((await request({...data,phone:"x"})).status,400);const one=await request(data);assert.equal(one.status,201);const created=await one.json();const two=await request(data);assert.equal(two.status,200);assert.equal((await two.json()).id,created.id);assert.equal((await request({...data,name:"Changed"})).status,409);assert.equal((await fetch(origin+"/api/crm/export")).status,503);assert.equal((await fetch(origin+"/api/uploads",{method:"POST",headers:{"Origin":origin,"x-lead-id":created.id,"x-upload-token":"invalid"},body:"x"})).status,401);console.log("PASS: 14 routes/assets + origin, validation, persistence, idempotency, conflict and access guards");
