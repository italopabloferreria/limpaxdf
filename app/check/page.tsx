import {SiteHeader} from "@/components/site-header";import {CheckForm} from "@/components/check-form";import {SiteFooter} from "@/components/site-footer";import {settings,captureMode} from "@/lib/config";export const dynamic="force-dynamic";export const metadata={title:"Prepare seu atendimento | Limpax Check"};export default function Page(){return <><SiteHeader/><main id="conteudo" className="check-page"><CheckForm mode={captureMode(settings())}/></main><SiteFooter/></>}
