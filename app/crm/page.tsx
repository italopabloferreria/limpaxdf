import {getChatGPTUser,chatGPTSignInPath} from "@/app/chatgpt-auth";
import {settings} from "@/lib/config";
import {database} from "@/lib/http";
import {CrmWorkspace} from "@/components/crm-workspace";
export const dynamic="force-dynamic";
export default async function CrmPage(){const user=await getChatGPTUser();const allowed=(settings().CRM_ADMIN_EMAILS||"").split(",").map(value=>value.trim().toLowerCase()).filter(Boolean);if(!user)return <main className="crm-gate"><p className="eyebrow">LIMPAX / CRM</p><h1>Acesso da equipe.</h1><p>Entre com uma conta autorizada para abrir a central de atendimentos.</p><a className="button" href={chatGPTSignInPath("/crm")}>Entrar no CRM</a></main>;if(!allowed.length||!allowed.includes(user.email.toLowerCase()))return <main className="crm-gate"><p className="eyebrow">LIMPAX / CRM</p><h1>CRM protegido.</h1><p>{allowed.length?"Esta conta não está na lista autorizada.":"Defina CRM_ADMIN_EMAILS nas configurações de produção para liberar a equipe."}</p></main>;const rows=await database(settings()).prepare("SELECT seq,id,payload,status,mode,created_at,updated_at,assigned_to,next_action_at FROM leads ORDER BY COALESCE(updated_at,created_at) DESC LIMIT 150").all<any>();const leads=rows.results.map(row=>{const payload=JSON.parse(row.payload);return {id:row.id,seq:row.seq,status:row.status,mode:row.mode,createdAt:row.created_at,updatedAt:row.updated_at||row.created_at,assignedTo:row.assigned_to,nextActionAt:row.next_action_at,contact:{name:payload.name||"Sem nome",phone:payload.phone||"",email:payload.email||"",problem:payload.problem||"outro",region:payload.region||"",property:payload.property||""}}});return <CrmWorkspace initialLeads={leads} operator={user.displayName}/>}
