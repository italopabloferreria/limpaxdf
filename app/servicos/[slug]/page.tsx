import {notFound} from "next/navigation";import {services} from "@/lib/content";import {SiteHeader} from "@/components/site-header";import {SiteFooter} from "@/components/site-footer";export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const slug=(await params).slug;const s=services.find(s=>s.slug===slug);return {title:s?.title||"Serviço",description:s?.copy}}export default async function Page({params}:{params:Promise<{slug:string}>}){const slug=(await params).slug;const service=services.find(s=>s.slug===slug);if(!service)notFound();return <><SiteHeader/><main id="conteudo" className="document-page"><p className="eyebrow yellow">{service.id} / {service.label}</p><h1>{service.title}</h1><p className="body-copy">{service.copy}</p><p className="review-notice">[VALIDAR] Serviço proposto no portfólio. Disponibilidade, escopo e descrição técnica sujeitos à confirmação pela Limpax.</p><h2>Prepare as informações.</h2><p>Informe o que está acontecendo, a região, o tipo de imóvel e as condições de acesso. Fotos são opcionais. Não é preciso conhecer o nome técnico do problema.</p><p>O Limpax Check organiza sua solicitação. Não substitui avaliação técnica e não confirma preço, prazo ou agendamento.</p><a href={"/check?problema="+service.problem} className="button">Preparar solicitação ↗</a><div className="actions"><a className="text-link" href="/#servicos">← Voltar aos serviços</a></div></main><SiteFooter/></>}

